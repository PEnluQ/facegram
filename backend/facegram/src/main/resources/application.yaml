server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/facegram
    username: postgres
    password: saymyname
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG